-- =====================================================================
-- V38: payments 테이블에 부분 취소 금액 컬럼 추가
-- =====================================================================
-- PaymentService.cancel()에서 부분 취소 시 누적 취소 금액을 저장한다.
-- 전액 취소 시 amount와 동일, 부분 취소 시 취소된 금액 합계.
-- =====================================================================

ALTER TABLE payments
    ADD COLUMN cancelled_amount DECIMAL(12, 2) NULL COMMENT '누적 취소 금액 (전액 취소 시 amount와 동일)';
